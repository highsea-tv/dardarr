{
    "name": "@dardarr/app",
    "version": "1.0.0",
    "type": "module",
    "private": true,
    "scripts": {
        "dev": "vite dev --host",
        "build": "vite build",
        "preview": "vite preview",
        "lint": "prettier --check . && eslint . && stylelint .",
        "format": "prettier --write . && eslint --fix . && stylelint --fix ."
    }
}